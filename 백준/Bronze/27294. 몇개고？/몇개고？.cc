#include <iostream>
using namespace std;

int main() {
	bool S; int T; cin >> T >> S;
	if (T < 12) { cout << 280; }
	else if (T < 17) { (S) ? cout << 280 : cout << 320; }
	else { cout << 280; }
	return 0;
}