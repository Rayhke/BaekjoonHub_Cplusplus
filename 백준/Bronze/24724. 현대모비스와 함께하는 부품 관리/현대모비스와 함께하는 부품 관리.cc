#include <iostream>
using namespace std;

int main(int argc, char* argv[]) {
	ios::sync_with_stdio(false); cin.tie(NULL);
	int T; cin >> T;
	for (int n = 1; n <= T; n++) {
		printf("Material Management %d\nClassification ---- End!\n", n);
	}
	return 0;
}